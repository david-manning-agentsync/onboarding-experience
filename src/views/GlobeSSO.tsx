/* PLACEHOLDER */
export default function GlobeSSO() {
  return (
    <div className="placeholder-view placeholder-view--manage">
      <div className="placeholder-view__badge label-uppercase">Manage (Salesforce) · Post-MVP</div>
      <h1 className="placeholder-view__title text-2xl font-bold">Globe — SSO</h1>
      <p className="placeholder-view__desc text-secondary">
        Single sign-on handoff pattern for Globe Life carrier systems. Producer is authenticated
        into the carrier portal from within AgentSync Manage without re-entering credentials.
      </p>
    </div>
  )
}
